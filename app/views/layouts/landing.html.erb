<!DOCTYPE html>
<head>
  
</head>
<body>

  <div class="body">
    <%= yield %>
  </div>
  
  <ul>
    <% Post.all.each do |post| %>
      <li><%= link_to_post(post) %></li>
    <% end %>
  </ul>
</body>
</html>